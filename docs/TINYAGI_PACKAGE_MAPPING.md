# pocketStudio vs TinyAGI packages mapping

Source inspected: <https://github.com/TinyAGI/tinyagi/tree/main/packages>  
Local upstream reference commit: `2db3a03a891b302c460f624d6c5a0523b8aa3528`  
Document date: 2026-05-01

This document maps the current `pocketStudio` Python implementation to the upstream TinyAGI `packages/` workspace. It is intentionally honest about gaps: the current project is an early Python backend plus an adapted TinyOffice frontend, not yet a full feature-for-feature TinyAGI port.

Status legend:

- `Implemented`: core behavior exists locally.
- `Partial`: a compatible slice exists, but TinyAGI has more behavior.
- `Stub`: endpoint/function returns placeholder data for UI compatibility.
- `Missing`: no local implementation yet.
- `Frontend only`: covered by copied/adapted `tinyoffice/`, not Python backend.

## Package-level Summary

| TinyAGI package | Upstream purpose | Current pocketStudio counterpart | Status |
|---|---|---|---|
| `packages/core` | Core config, queue DB, routing, agent invocation, adapters, memory, pairing, plugins, schedules, response handling | `pocketStudio/core`, `pocketStudio/models.py`, `pocketStudio/services`, `pocketStudio/providers` | Partial |
| `packages/server` | HTTP API and SSE server for TinyOffice and CLI control | `pocketStudio/main.py`, `pocketStudio/api/*` | Partial |
| `packages/teams` | Team routing, group conversation, chatroom orchestration | `pocketStudio/services/orchestrator.py`, `team_service.py`, `chat_service.py` | Partial |
| `packages/channels` | Discord, Telegram, WhatsApp, SSE client channel bridges | `pocketStudio/channels`, `ChannelService`, response queue delivery foundations | Partial |
| `packages/main` | Long-running daemon, queue processor loop, heartbeat, channel process manager | FastAPI lifespan plus `WorkerService`; local daemon pid/log supervisor via CLI; manual `POST /api/queue/process-next` remains available; worker health/maintenance APIs exist | Partial |
| `packages/cli` | `tinyagi` CLI for install/start/status/agents/teams/channels/logs/pairing/schedules | `pocketStudio/cli.py`, `pocketstudio` console script | Partial |
| `packages/visualizer` | Terminal visualizers for teams and chatroom | `pocketStudio/visualizer.py`, `pocketstudio visualize`, `pocketstudio chatroom`, plus adapted `tinyoffice/` web UI | Partial |

Approximate Python backend reproduction ratio, excluding copied/adapted `tinyoffice/` frontend code:

| TinyAGI package | Python reproduction ratio | Current priority |
|---|---:|---|
| `packages/core` | ~77% | Continue system runtime parity before channels/provider breadth. |
| `packages/server` | ~84% | Continue control-plane/system APIs and operational safety. |
| `packages/teams` | ~65% | Core routing/chatroom behavior is usable; remaining work is mostly exact upstream session/streaming semantics. |
| `packages/channels` | ~27% | Telegram text bridge now exists; Discord/WhatsApp/SSE client and richer media delivery remain deferred. |
| `packages/main` | ~54% | Continue daemon/worker/process recovery without channel manager first. |
| `packages/cli` | ~53% | Continue system/admin commands and implemented channel controls before interactive setup. |
| `packages/visualizer` | ~55% Python | Terminal dashboard and chatroom polling are implemented; interactive Ink-style input remains deferred. |

## `packages/core`

Upstream `@tinyagi/core` is the heart of the system. It owns persistent config, SQLite queues, agent execution, routing, events, plugins, pairing, schedules, memory, and response/file handling.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `core/src/types.ts` | Shared TypeScript types for agents, teams, messages, settings, queues, schedules, etc. | `pocketStudio/models.py` | Partial | Python models exist for agents, teams, messages, tasks, chat, events, projects, schedules, responses, pairing, and custom provider slices. Missing full TinyAGI channel and attachment types. |
| `core/src/config.ts` | `TINYAGI_HOME`, settings file, default settings, agents/teams resolution, model resolution | `pocketStudio/core/config.py`, `SettingsService`, `AgentService`, `TeamService`, `ProviderRegistry` | Partial | pocketStudio now uses `.pocketStudio/settings.json` as the primary settings store, keeps SQLite `app_settings` as a compatibility mirror/migration source, syncs agent/team CRUD into the file, validates imported/updated settings sections and common field types, expands setup/settings `~` and `$HOME` paths for workspace and agent working directories, backs up current settings before writes, exposes backup metadata, supports explicit backup restore, and backs up/repairs common malformed JSON. New agents inherit the monitoring heartbeat interval. Env prefix is `POCKETSTUDIO_`; full npm `jsonrepair` parity remains incomplete. |
| `core/src/queues.ts` | SQLite queue schema and operations: incoming messages, processing, outgoing responses, dead messages, agent messages, chat messages, queue status, pruning/retry | `pocketStudio/core/database.py`, `QueueService`, `ChatService`, `EventService` | Partial | Has messages, responses, chat, events, retries, agent history, stale recovery, dead-letter retry/delete with missing-id 404s, generic failed/dead retry, lifecycle guards that prevent reprocessing done/running/dead messages, TinyAGI-like queue status counters (`pending`, `completed`, `dead`, `responsesPending`), queue diagnostics for oldest queued/running age, retryable/exhausted/stale processing counts, processing duration/status payloads, enriched dead-message payloads, per-agent queue status, response acking, client `messageId` idempotency lookup for `/api/message`, chat/message pruning, and response pruning. Still missing exact TinyAGI schema parity and channel delivery integrations. |
| `core/src/invoke.ts` | Spawn CLI harnesses, stream process output, kill active agent processes, invoke agents with provider adapters | `pocketStudio/providers/subprocess.py`, `pocketStudio/providers/*`, `Orchestrator._run_agent`, `/api/processes` | Partial | Subprocess harness, process registry, timeout, stdout line parsing, kill hook, active process snapshots, basic process metadata, and `agent.process` events now exist. Missing full TinyAGI streaming event taxonomy and richer lifecycle/status projection. |
| `core/src/adapters/types.ts` | Adapter contract for agent harnesses | `pocketStudio/providers/base.py`, `pocketStudio/providers/subprocess.py` | Partial | Basic provider request/response, raw metadata, process metadata, active process snapshot fields, and subprocess harness abstraction exist. Missing full TinyAGI streaming/process lifecycle fields. |
| `core/src/adapters/claude.ts` | Claude Code adapter | `pocketStudio/providers/cli_agent.py` `ClaudeProvider` | Partial | Basic Claude subprocess harness exists using `claude --print --output-format stream-json --verbose`, JSON/text extraction, process registry, and kill support. Missing advanced resume/session controls and exact upstream event forwarding. |
| `core/src/adapters/codex.ts` | Codex adapter | `pocketStudio/providers/codex.py` | Partial | Codex provider now follows the upstream-style non-interactive `codex exec resume --last ... --json -` flow by default, passes prompts over stdin for Windows safety, supports reset without `resume --last`, maps `openai` to Codex while retaining `codex` as a compatibility alias, resolves Windows npm shims such as `codex.CMD`, injects agent role/system prompt plus prior context into the prompt, parses several Codex JSON/JSONL output shapes, exposes process metadata/kill support, falls back to PowerShell/synchronous subprocess execution on Windows command-resolution, permission, or asyncio pipe failures, forwards stdin correctly through the PowerShell fallback, and supports custom provider command-line overrides through stored provider config. Missing exact upstream streaming event forwarding and richer session controls. |
| `core/src/adapters/opencode.ts` | OpenCode adapter | `pocketStudio/providers/cli_agent.py` `OpenCodeProvider` | Partial | Basic OpenCode subprocess harness exists using `opencode run --print`, stdout/JSON extraction, process registry, and kill support. Missing advanced session controls and exact upstream event forwarding. |
| `core/src/adapters/index.ts` | Adapter registry/selection | `pocketStudio/providers/registry.py` | Partial | Registry now exposes built-in `local`, upstream-compatible `openai`/`anthropic`, compatibility aliases `codex`/`claude`, and `opencode`, plus SQLite-backed custom OpenAI-compatible/Codex/Claude/OpenCode provider entries. Custom providers are prevented from shadowing built-in provider IDs such as `local`, so dry-run/local tests and fallback behavior remain stable. It also reports provider diagnostics including provider alias vs underlying harness (`openai` -> Codex), command resolution, base args, active processes, default Codex home/session writability, and Windows PowerShell fallback availability. Custom Codex provider `base_url` is treated as a command line for alternate Codex binaries/args. Missing exact upstream adapter configuration shape and richer harness options. |
| `core/src/router.ts` | Parse `@agent`/`@team`, route messages, find teams, mention handling | `Orchestrator._parse_target`, `_dispatch` | Partial | Supports direct `@agent:id`, `@team:id`, bare team id. Missing rich mention parsing, default routing, group/team membership routing, forwarding between agents. |
| `core/src/agent.ts` | Agent directory setup, AGENTS.md/system prompt, skills sync, built-in instructions | `AgentService` workspace helpers and compatibility routes | Partial | Creates pocketStudio workspace directories, AGENTS.md, heartbeat, `.pocketStudio/SOUL.md`, memory dir, skills dir, `.claude/skills` symlink/fallback, prompt file read/write, prompt composition helper, idempotent workspace status/repair checks, and project workspace root selection for agent execution when `projectId` is provided. Missing upstream template copying and real bundled skill sync. |
| `core/src/memory.ts` | Scan agent memory directories and format memory index | `AgentService.load_memory_index`, `scan_memory_tree`, `/api/agents/{id}/memory`, `/api/agents/{id}/memory/file` | Partial | Scans markdown frontmatter for `name` and `summary`, skips hidden files, returns TinyAGI-like hierarchical `tree`, formats the memory index, and exposes safe backend read/write/delete for `.md` files under `memory/` with traversal/hidden-path guards. Missing memory skill behavior and richer YAML parsing. |
| `core/src/plugins.ts` | Plugin loading and incoming/outgoing hooks/events | `PluginService`, `/api/plugins`, queue/response hook integration, `EventService` listeners | Partial | JSON-config plugin discovery plus executable `plugin.py` modules now exist. Python plugins can expose `hooks` for incoming/outgoing transforms and `activate(ctx)` for event handlers/logging, and `EventService.emit()` broadcasts non-plugin events to registered plugin handlers. Missing JS/TS module loading and full TinyAGI plugin API parity. |
| `core/src/pairing.ts` | Pairing state for external channel senders | `ChannelService`, `pocketStudio/api/compat.py`, pairing tables | Partial | Pairing persistence/API and sender enforcement for `/api/message` exist. Missing real channel clients and channel-specific delivery integration. |
| `core/src/schedules.ts` | Cron and one-shot schedules | `ScheduleService`, `WorkerService`, `pocketStudio/api/compat.py` | Partial | Schedule CRUD, unique labels, id-or-label lookup for delete/fire, next-fire preview (`nextFireAt`, `dueInMs`, `due`), strict supported-range validation for the local five-field cron subset, worker-driven due execution with just-fired message priority, and manual fire/update-state flow exist for one-shot schedules and simple five-field cron. Missing full Croner expression parity, live job registry, and timezone/named-field support. |
| `core/src/response.ts` | Long-response splitting, file tag collection, response streaming | `ResponseService`, `QueueService.enqueue_responses_from_message`, `QueueService.enqueue_response`, `/api/responses`, `/api/responses/pending` | Partial | Long response preview/file persistence, `[send_file: ...]` collection, outgoing hook metadata, response queue projection, ack/prune with missing-id 404s, proactive response enqueue, pending-response-by-channel projection, and richer `response:queued` event payloads exist. Missing exact upstream streamResponse function shape, provider stdout/stderr streaming, and real channel delivery policies. |
| `core/src/logging.ts` | Log file plus in-process event bus | `EventService`, `.pocketStudio/logs/pocketstudio.log`, `/api/events`, `/api/events/stream`, `/api/logs` | Partial | Events are persisted in SQLite, appended to a log file, broadcast to in-memory listeners, exposed through listener-backed SSE with replay/keepalive, mapped to TinyOffice/TinyAGI-style event names (`message:incoming`, `agent:progress`, `response:queued`, etc.), and available as structured log records with event-type/text filtering. Missing severity-specific logging APIs, log rotation, and channel/service-specific log files. |
| `core/src/ids.ts` | Nanoid id generation | `pocketStudio/core/ids.py` | Partial | Shared URL-safe `nanoid` and prefixed ID helpers now back project, comment, and schedule IDs. Queue/task rows still use SQLite autoincrement for local compatibility, so exact TinyAGI ID parity is incomplete. |
| `core/src/index.ts` | Barrel exports | Python package imports | Implemented in spirit | No exact equivalent needed in Python yet. |

## `packages/server`

Upstream `@tinyagi/server` exposes the API that TinyOffice expects. pocketStudio uses FastAPI and currently implements a subset.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `server/src/index.ts` | Hono server setup, route registration, status root | `pocketStudio/main.py` | Partial | FastAPI app exists with CORS, static UI, API routers. Missing full TinyAGI route surface. |
| `server/src/sse.ts` | Server-sent events stream | `pocketStudio/api/system.py` `/api/events/stream`, `/api/events/office`, `EventService.office_event` | Partial | SSE now subscribes to the in-process event bus instead of polling only, supports replay from `since`, sends keepalives, and maps queue/agent/process/response/team events to TinyOffice-style event names. `/api/events/office` exposes the same mapped event projection for polling/debug tools. Missing exact upstream streaming payloads for provider stdout/stderr and richer replay pagination. |
| `server/src/tasks-db.ts` | Tasks/projects/comments persistence helpers | `TaskService`, `ProjectService`, task/project/comment SQLite tables | Partial | Tasks, projects, optional project working directories, comments, comment counts, assignee/project fields, persisted reorder, project prefix/color generation, TinyAGI-style task `number`/`identifier`, and missing-id delete guards now exist. Explicit project working directories are execution cwd targets only, not agent skill/memory/state stores; projects without a workspace leave agents in their own configured workspace. Still missing exact separate `tasks.db` layout and JSON migration parity. |
| `server/src/routes/agents.ts` | Agent CRUD, custom provider config, settings-backed agents | `pocketStudio/api/agents.py`, `pocketStudio/api/compat.py` | Partial | Agent CRUD, upstream-style `PUT /api/agents/{id}` create/update with `working_directory`, `system_prompt`, model/provider, and heartbeat projection, heartbeat settings, workspace status/repair, memory file read/write/delete, reset, and custom provider compatibility routes exist. Missing richer reset semantics for external harness sessions and exact custom-provider storage shape. |
| `server/src/routes/teams.ts` | Team CRUD and settings-backed teams | `pocketStudio/api/teams.py` | Partial | Team CRUD exists with `chain`/`fanout`, persisted `leader_agent`, compatibility `leader_agent` settings shape, member add/remove/leader mutation routes, and upstream-style `PUT /api/teams/{id}` updates for `name`, `agents`, `leader_agent`, `maxRounds`, and `stopWhenIdle` while syncing settings JSON. |
| `server/src/routes/messages.ts` | Incoming message endpoint, dispatch queue enqueue | `pocketStudio/api/messages.py` `/api/messages`, `pocketStudio/api/compat.py` `/api/message` | Partial | Local route is `/api/messages`; compatibility `/api/message` now enforces pairing for external channels, supports sticky default targets and `projectId`, preserves the current numeric queue-id response contract, stores caller-supplied client `messageId` in metadata, and rejects duplicate client `messageId` values with an upstream-like `409` payload. Missing attachments, exact upstream string message-id storage, and full channel metadata. |
| `server/src/routes/queue.ts` | Queue status, processing, kill, dead/retry/delete, response queue controls | `pocketStudio/api/messages.py`, `pocketStudio/api/compat.py`, `QueueService` | Partial | Queue list/get/process-next/status, `/api/queue/diagnostics`, upstream-compatible status aliases (`pending`, `completed`, `dead`, `responsesPending`), generic failed/dead retry, dead retry/delete with upstream-like 404s for missing dead messages, enriched dead-message payloads (`data`, `failedReason`, `attemptsMade`, `timestamp`), stale recovery, oldest queued/running diagnostics, processing list with started/duration metadata, kill-like reset, proactive response enqueue, pending responses by channel, response ack/prune with missing-id 404s, and queue/message pruning exist. Missing exact TinyAGI processing lifecycle and channel delivery coupling. |
| Process lifecycle routes | Active harness process list/kill | `ProviderRegistry`, `ProcessRegistry`, `pocketStudio/api/compat.py` `/api/processes`, `/api/processes/{agent_id}/kill`, `/api/providers/diagnostics` | Partial | System-level active agent process snapshots expose agent, pid, command, args, cwd, started time, duration, and alive state, with direct kill by agent id. Provider diagnostics expose CLI command resolution and fallback status for debugging Codex/Claude/OpenCode harness setup. Missing full upstream process recovery/session lifecycle. |
| `server/src/routes/agent-messages.ts` | Per-agent and global conversation history | `QueueService`, `pocketStudio/api/compat.py` | Partial | `agent_messages` table, per-agent `/api/agents/{id}/messages`, and upstream-compatible global `/api/agent-messages` now exist with limit and `since_id` filtering. Missing full upstream message metadata and channel-specific history semantics. |
| `server/src/routes/chatroom.ts` | Team chatroom messages | `pocketStudio/api/chat.py`, `ChatService`, `QueueService` | Partial | Chatroom list/post exists and POST now follows upstream semantics more closely: default sender is `user`, the user message is persisted once, existing team members receive per-agent internal `channel=chatroom` queue messages instead of a synthetic `@team` chain message, and responses expose upstream-compatible `from_agent` while preserving local `sender` compatibility. Missing real channel outbound delivery coupling. |
| `server/src/routes/chats.ts` | Chat archive/listing APIs | `ChatService.archives`, `/api/chats`, `/api/chats/{team_id}` | Partial | SQLite-backed chat archive listing/read endpoints exist with counts, latest sender/message summary, sender/text filters, and optional maintenance pruning. Missing Markdown file archive parity with upstream `CHATS_DIR`. |
| `server/src/routes/logs.ts` | Read log files | `EventService.log_lines`, `EventService.log_records`, `pocketStudio/api/compat.py` `/api/logs` | Partial | File-backed `/api/logs` returns legacy `lines` plus structured `records`, with `event_type` and `contains` filters. Missing log rotation and channel/service-specific log selection. |
| `server/src/routes/settings.ts` | GET/PUT settings | `SettingsService`, `pocketStudio/api/compat.py` `/api/settings`, `/api/settings/export`, `/api/settings/import`, `/api/settings/validate`, `/api/settings/preview`, `/api/settings/backup`, `/api/settings/restore-backup`, `/api/setup` | Partial | Backend now persists workspace/channels/models/monitoring sections to `.pocketStudio/settings.json`, overlays live SQLite agents/teams/custom providers for responses, supports JSON import/export, dry-run validation, diff/preview before import, backup inspection, backup restore, and upstream-like setup path expansion for workspace and agent working directories, rejects invalid section/field types plus invalid agent/team/custom-provider apply plans before writing, backs up current settings before writes, and repairs common JSON mistakes with a `.bak` backup. Full `jsonrepair` parity remains incomplete. |
| `server/src/routes/services.ts` | Start/stop/restart services/channels/status | `pocketStudio/api/compat.py` service/status routes, `WorkerService`, `pocketStudio/channels/telegram.py`, `SettingsService` | Partial | Worker start/stop/pause/resume/restart/status, service-level `/api/services/status/start/stop/restart/apply`, Telegram channel `/api/services/channel/telegram/{status,start,stop,restart,tick}`, health, queue summary, last processed/error timestamps, maintenance recover/prune action for stale messages, responses, completed messages, and optional chat archives, uptime, settings-backed channel status with implemented/unimplemented flags, and apply reporting exist. Still missing daemon process supervisor and non-Telegram channel process manager. |
| `server/src/routes/pairing.ts` | Pairing state endpoints | `ChannelService`, `pocketStudio/api/compat.py`, pairing tables | Partial | Pairing list/approve/revoke/dismiss exists and `/api/message` uses `ensureSenderPaired` for external senders. Missing real channel client integration. |
| `server/src/routes/projects.ts` | Project CRUD | `ProjectService`, `pocketStudio/api/compat.py` | Partial | Project CRUD, optional project working-directory assignment/status/repair/clear, status filtering, task counts, auto prefix/color defaults, single-project lookup, missing-id delete 404s, and task detachment/renumbering on project delete now exist. Project directories are created only when configured and no agent `memory/`, `.agents`, `AGENTS.md`, or `.pocketStudio` metadata is installed there; clearing a project workspace restores agent-workspace fallback for project-bound runs. Missing exact timestamp/storage layout parity. |
| `server/src/routes/tasks.ts` | Task CRUD, comments, reorder | `pocketStudio/api/tasks.py`, `TaskService`, `ProjectService` comments | Partial | Task CRUD, comments, comment counts, server-side project/status/assignee/search filters, project-scoped task numbers, computed identifiers, persisted reorder, missing-id delete 404s, and upstream-style `{ ok, task }` envelope compatibility on create/update now exist while preserving top-level task fields for the current frontend. Missing exact string task IDs and separate task database parity. |
| `server/src/routes/schedules.ts` | Schedule CRUD | `ScheduleService`, `WorkerService`, `pocketStudio/api/compat.py` | Partial | Schedule CRUD, `POST /api/schedules/validate`, next-fire status projection, id-or-label delete with missing-id 404s, manual `POST /api/schedules/{id}/fire`, and worker-driven due execution exist. Missing exact Croner behavior, live job registry, and complete validation parity. |

## `packages/teams`

Upstream `@tinyagi/teams` provides higher-level routing and collaboration behavior for teams.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `teams/src/routing.ts` | Route messages to agents/teams and manage mentions | `pocketStudio/services/team_routing.py`, `Orchestrator._parse_target`, `_dispatch`, `_handle_team_tags`, `_handle_direct_agent_team_tags`, `pocketStudio/api/teams.py` | Partial | Shared bracket-depth parser, tag stripping, readable tag conversion, target parsing, leader-first ordering, `[@agent: ...]` mention extraction, comma-separated teammate fanout, case-insensitive teammate/team tag matching, duplicate mention suppression, direct-agent team-context resolution, chatroom broadcast fanout, internal enqueue, parent channel/sender metadata propagation, and upstream-style team config updates exist. |
| `teams/src/conversation.ts` | Multi-agent conversation flow | `Orchestrator._run_team`, `_run_iterative_rounds`, direct-agent team tag handling, `QueueService.enqueue_responses_from_message` | Partial | Chain/fanout, leader-first ordering, shared-context mention routing, mention-driven teammate follow-up messages, controlled iterative rounds (`max_rounds`, `stop_when_idle`), stateless direct-agent response handling for team mentions/chatroom tags, and response projection that converts internal team tags into readable text exist. Team chain handoffs are no longer stored as `role=user` agent history, so teammate output is not shown as user input in conversation panes. Missing exact upstream response streaming/session behavior and real channel delivery coupling. |
| `teams/src/chatroom.ts` | Shared team chatroom behavior | `ChatService`, `pocketStudio/api/chat.py`, `_handle_team_tags`, `_broadcast_chatroom`, `QueueService.grouped_chatroom_messages`, `/api/queue/grouped` | Partial | Chatroom persistence plus upstream-like POST fanout to per-agent `channel=chatroom` internal messages now exist, including upstream-compatible `from_agent` API projection. Bracket-depth `[#team: ...]` chatroom broadcasts from team-routed and direct-agent outputs enqueue internal chatroom messages for other teammates with parent channel/sender metadata, and consecutive chatroom queue items can be grouped into TinyAGI-like batches. Missing full original channel outbound delivery integration. |
| `teams/src/index.ts` | Barrel exports | Python imports | Implemented in spirit | No exact Python barrel needed. |

## `packages/channels`

Upstream `@tinyagi/channels` connects external message sources to the core queue and outgoing response queue.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `channels/src/default-agent.ts` | Default target agent logic for channels | `ChannelService`, `pocketStudio/channels/telegram.py` | Partial | Sticky default agent/team routing exists for compatibility `/api/message` and Telegram inbound messages. Missing exact file-backed settings parity. |
| `channels/src/discord.ts` | Discord bot inbound/outbound bridge, files, pairing | None | Missing | Requires Discord integration and outgoing queue. |
| `channels/src/telegram.ts` | Telegram bot inbound/outbound bridge, files, pairing | `pocketStudio/channels/telegram.py`, `/api/services/channel/telegram/*`, `pocketstudio channel * telegram` | Partial | Python Telegram Bot API bridge exists with token lookup from `TELEGRAM_BOT_TOKEN` or `channels.telegram.bot_token`, long-poll inbound tick/loop, private-chat text/caption handling, pairing enforcement, `/agent` and `/team` commands, default target routing, queue enqueue with Telegram metadata, pending response delivery, long-message splitting, response ack, status/start/stop/restart/tick service routes, and CLI channel controls. Missing media download/upload parity, Markdown fallback behavior, restart shell integration, and SSE-triggered delivery optimization. |
| `channels/src/whatsapp.ts` | WhatsApp Web inbound/outbound bridge, media, pairing | None | Missing | Requires WhatsApp integration and outgoing queue. |
| `channels/src/sse-client.ts` | SSE client channel bridge | None | Missing | Backend has SSE server; Telegram currently polls pending responses during its channel loop/tick rather than using a separate SSE client. |

## `packages/main`

Upstream `@tinyagi/main` is the daemon that continuously processes queue messages, launches server, channels, and heartbeat.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `main/src/index.ts` | Daemon entrypoint, queue loop, process messages, direct responses, shutdown | `pocketStudio/main.py`, `WorkerService`, `Orchestrator`, `EventService`, `/api/worker/*`, `/api/services/*`, `/api/processes`, `/api/events/stream`, `pocketstudio daemon *`, `pocketstudio worker *`, `pocketstudio process *` | Partial | FastAPI lifespan starts/stops a background worker that recovers stale messages, fires due schedules/heartbeat, prioritizes messages fired in the current tick, continues past poison queue items until a processable message completes or the tick is exhausted, and processes normal queued messages. Worker/service status includes health, queue summary, queue diagnostics, last tick/processed/error metadata, pause/resume state, channel implementation flags, and start/stop/restart/apply controls. Maintenance can recover stale messages plus prune completed/acked/chat records, forced tick can process while paused and prefers the newest ordinary queued item after system-triggered work, schedules can be manually fired for control-plane recovery/debugging, active harness processes can be listed/killed by agent id, and runtime events are pushed over listener-backed SSE. CLI can start/stop/restart/status the local API process with pid/log files. Missing external process recovery and channel process orchestration. |
| `main/src/channels.ts` | Channel process manager | None | Missing | No external channel processes. |
| `main/src/heartbeat.ts` | Periodic heartbeat messages | `HeartbeatService`, `WorkerService`, `/api/heartbeat/status`, `/api/heartbeat/tick`, `/api/heartbeat/state`, `/api/agents/{id}/heartbeat` | Partial | Heartbeat loop reads each agent workspace `heartbeat.md`, respects per-agent enabled/interval config, queues due messages with heartbeat metadata, records last sent state, reports per-agent `nextDueAt`/`dueInMs`/`due`, exposes base interval/due counts, supports manual forced tick, and can clear last-sent state for recovery/debugging. Missing exact settings-file parity and richer frontend controls. |

## `packages/cli`

Upstream `@tinyagi/cli` provides the `tinyagi` command. pocketStudio now has a basic HTTP-backed `pocketstudio` CLI.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `cli/bin/tinyagi.mjs` | CLI executable | `pocketStudio/cli.py`, `pyproject.toml` console script | Partial | `pocketstudio` command exists after package install. |
| `cli/src/daemon.ts` | start/stop/status/restart/open office | `pocketstudio daemon *`, `pocketstudio status`, `pocketstudio worker *`, `pocketstudio heartbeat *` | Partial | Local API daemon process supervisor exists with pid/log files and `uvicorn pocketStudio.main:app` spawning, plus `pocketstudio daemon open` for opening the local Office/API URL. Worker CLI covers status/start/stop/pause/resume/restart/tick/maintenance, and heartbeat CLI covers status/tick/clear. Missing richer process recovery. |
| `cli/src/install.ts` | installer/bootstrap | None | Missing | No install flow. |
| `cli/src/agent.ts` | agent add/remove/list/show/provider/reset/custom providers | `pocketstudio agent *`, FastAPI agent endpoints | Partial | CLI covers list/add/show/remove/reset/workspace/repair-workspace. Missing richer interactive provider selection. |
| `cli/src/team.ts` | team add/remove/list/show/member management | `pocketstudio team *`, FastAPI team endpoints | Partial | CLI covers list/add/show/remove, `--leader`, `--max-rounds`, `--keep-alive`, add-member, remove-member, and set-leader. |
| `cli/src/channel.ts` | channel start/stop/restart | `pocketstudio channel *` | Partial | CLI can call service channel status/start/stop/restart/tick for implemented channels such as Telegram. Discord/WhatsApp process management remains missing. |
| `cli/src/messaging.ts` | send messages, reset channel queues, setup channels | `pocketstudio send` | Partial | CLI can send messages. No channel setup/reset commands. |
| `cli/src/logs.ts` | log viewing | `pocketstudio logs`, `/api/logs` | Partial | CLI log viewing exists with `--event-type` and `--contains` filters. Missing follow/tail streaming and service-specific log selection. |
| Settings CLI commands | Settings inspection/import/export/validation | `pocketstudio settings get/export/import/validate/preview/backup/restore-backup` | Partial | CLI can read, export, validate, preview import diffs, import, inspect backup status, and restore settings JSON through the API. Missing interactive setup and exact settings file editing. |
| Process CLI commands | Active process inspection/kill | `pocketstudio process list/kill` | Partial | CLI can list active agent harness processes and kill one by agent id. Missing richer session controls and streaming process output. |
| `cli/src/pairing.ts` | approve/list/revoke pairings | `pocketstudio pairing *` | Partial | CLI covers list/approve/revoke. |
| `cli/src/provider.ts` | provider/model config commands | `pocketstudio provider *` | Partial | CLI covers list/custom/save/remove for custom providers. Missing interactive flow. |
| `cli/src/schedule.ts` | schedule list/create/delete | `pocketstudio schedule *` | Partial | CLI covers list/add/remove/fire, including forced manual fire for disabled schedules. |
| Worker maintenance CLI commands | Queue/worker maintenance operations | `pocketstudio worker status/start/stop/pause/resume/restart/tick/maintenance`, `pocketstudio queue diagnostics` | Partial | CLI can control the worker, pause/resume queue consumption, run one tick, inspect queue diagnostics, recover stale messages, and prune completed/acked/chat records through backend endpoints. |
| Task/project CLI commands | TinyOffice/control-plane task and project operations | `pocketstudio task *`, `pocketstudio project *` | Partial | CLI now covers task list/show/add/update/comments/comment/reorder/remove and project list/show/add/update/remove plus project working-directory assignment/status/repair. |
| `cli/src/shared.ts` | shared settings/prompt helpers | `Settings`, README | Partial/Missing | No interactive prompts. |
| `cli/src/update.ts` | update command | None | Missing | Out of scope for backend. |
| `cli/src/version.ts` | version command | `pocketstudio version` | Partial | CLI prints installed package metadata, with an editable-source fallback when package metadata is unavailable. |

## `packages/visualizer`

Upstream `@tinyagi/visualizer` provides terminal visualizations. The current project includes a Python/stdout terminal visualizer wired to the local FastAPI API, plus the copied/adapted web `tinyoffice/` frontend.

| TinyAGI file | Upstream responsibility | pocketStudio mapping | Status | Notes / gaps |
|---|---|---|---|---|
| `visualizer/src/team-visualizer.tsx` | Ink terminal team visualizer | `pocketStudio/visualizer.py`, `pocketstudio visualize`, `tinyoffice/` web UI | Partial | Python terminal dashboard polls `/api/agents`, `/api/teams`, `/api/events/office`, and `/api/queue/status`; it does not depend on Ink. |
| `visualizer/src/chatroom-viewer.tsx` | Ink terminal chatroom viewer | `pocketStudio/visualizer.py`, `pocketstudio chatroom`, `ChatService`, `tinyoffice/` web UI | Partial | Python terminal viewer polls `/api/chatroom/{team}` and supports `--send`; interactive character-by-character input is not implemented. |

## Copied TinyOffice Frontend

Although not under `packages/`, `tinyoffice/` is also part of the upstream repository. The current project includes a copied Next.js TinyOffice frontend in `tinyoffice/` and adapts it to the local Python API mostly through `tinyoffice/src/lib/api.ts`.

| Upstream TinyOffice area | pocketStudio mapping | Status |
|---|---|---|
| Office scene and dashboard pages | `tinyoffice/src/app/*`, `tinyoffice/src/components/*` | Copied/adapted |
| API client | `tinyoffice/src/lib/api.ts` | Adapted with real agent/settings/task/project/comment/schedule/control-plane calls, real queue status/processing/kill/response endpoints, and compatibility transforms |
| Static assets | `tinyoffice/public/*` | Copied |
| Built-in simple static UI | `pocketStudio/static/*` | Separate fallback UI, not upstream TinyOffice |

## Current Python Backend Coverage

The current Python backend has these implemented slices:

- FastAPI app and CORS/static hosting: `pocketStudio/main.py`
- SQLite schema and lightweight database wrapper: `pocketStudio/core/database.py`
- Settings/env/home paths, primary `.pocketStudio/settings.json` persistence, validation, import preview/diff, JSON import/export, backup inspection, and backup restore: `pocketStudio/core/config.py`, `pocketStudio/services/settings_service.py`
- Pydantic data contracts: `pocketStudio/models.py`
- Agent CRUD, workspace setup/status/repair, memory tree scanning/file management, and prompt composition: `pocketStudio/services/agent_service.py`, `pocketStudio/api/agents.py`
- Team CRUD with leader support: `pocketStudio/services/team_service.py`, `pocketStudio/api/teams.py`
- Message queue CRUD/status/process-next/diagnostics: `pocketStudio/services/queue_service.py`, `pocketStudio/api/messages.py`, `pocketStudio/api/compat.py`
- Chain/fanout/leader orchestration and teammate mention enqueue: `pocketStudio/services/orchestrator.py`
- Local echo, OpenAI-compatible provider abstraction, callable Codex exec provider, Claude/OpenCode subprocess harnesses, provider command diagnostics, and SQLite-backed custom provider registry: `pocketStudio/providers/*`
- Active agent harness process registry, process snapshots, and direct kill endpoints: `pocketStudio/providers/subprocess.py`, `pocketStudio/providers/registry.py`, `pocketStudio/api/compat.py`
- Chatroom persistence: `pocketStudio/services/chat_service.py`, `pocketStudio/api/chat.py`
- TinyAGI-style task/project CRUD, optional project working-directory assignment/status/repair, explicit project root cwd selection for agent execution without installing agent skill/memory/state files there, agent-workspace fallback when a project has no workspace, filtering, comments, task counts, and project-delete task detachment: `pocketStudio/services/task_service.py`, `pocketStudio/services/project_service.py`, `pocketStudio/api/tasks.py`
- Persisted event stream, plugin event listeners, TinyOffice-style event mapping, mapped event polling, listener-backed SSE replay/keepalive, and SSE compatibility: `pocketStudio/services/event_service.py`, `pocketStudio/api/system.py`
- TinyOffice/TinyAGI compatibility endpoints: `pocketStudio/api/compat.py`
- Project/comment persistence: `pocketStudio/services/project_service.py`
- Schedule persistence, validation, id-or-label management, due execution, manual fire, and next-fire preview: `pocketStudio/services/schedule_service.py`
- Per-agent/global message history, response projection, pending response query, proactive response enqueue, and response lifecycle events: `pocketStudio/services/queue_service.py`
- Long-response/file-tag response preparation and plugin outgoing hooks: `pocketStudio/services/response_service.py`
- Background queue worker daemon with health/status, pause/resume, forced tick, heartbeat tick/state control, and maintenance actions: `pocketStudio/services/worker_service.py`, `pocketStudio/services/heartbeat_service.py`
- SQLite-backed chat archive filtering, summaries, and pruning: `pocketStudio/services/chat_service.py`

## Highest-priority Missing Backend Work

To move toward a more complete Python rewrite of TinyAGI, the next backend milestones should be:

1. Full TinyAGI-compatible settings service: `/api/settings` now uses `.pocketStudio/settings.json` as the primary store, mirrors legacy SQLite app settings, syncs agent/team CRUD into the file, supports validated JSON import/export, dry-run validation, import diff/preview, backup inspection, backup restore, and setup/settings `~`/`$HOME` path expansion for workspace and agent working directories, rejects invalid section/field types plus invalid agent/team/custom-provider apply plans before writing, and repairs common malformed JSON with backup; full npm `jsonrepair` behavior still needs parity.
2. Queue parity: queue status with upstream-compatible aliases, outgoing responses, proactive response enqueue, pending response query by channel, response acking, responses projection, agent message history, lifecycle guards, generic retry, enriched dead-letter payloads, dead-letter retry/delete, stale recovery, processing duration/status payloads, per-agent status, and pruning now exist; exact schema parity and channel delivery integrations still need work.
3. Background daemon loop: continuous queue processing, stale recovery, just-fired schedule/heartbeat priority, poison-message tolerance inside a worker tick, worker and service control routes, pause/resume, health/queue summary, queue diagnostics, service status/start/stop/restart/apply, maintenance recover/prune action for queue/response/chat records, uptime status, active process list/kill, listener-backed SSE runtime event push, and CLI pid/log process supervision now exist; richer process recovery and channel process orchestration still need work.
4. Real agent harness adapters: subprocess abstraction plus callable Codex exec provider, basic Claude/OpenCode adapters, process registry/kill support, process metadata, prompt/context injection, and custom command-line overrides now exist; richer streaming, reset/resume controls, and exact upstream event forwarding still need work.
5. Agent workspace parity: AGENTS.md/system prompt files, placeholder skill install, workspace initialization, workspace health/repair checks, `.pocketStudio/SOUL.md`, `.claude/skills` symlink/fallback, hierarchical memory tree loading, and safe memory file read/write/delete now exist; upstream template syncing and real skill registry still need work.
6. Projects/tasks/comments parity: projects, optional project working directories with explicit clear/fallback behavior, comments, task project/assignee fields, project prefix/color defaults, TinyAGI-style task numbers/identifiers, task counts, server-side filters/search, project-delete task detachment/renumbering, persisted reorder, and direct project root cwd execution for project-bound agent runs with explicit workspace now exist; projects without workspace keep the agent's own workspace. Exact string task IDs, separate `tasks.db`, and JSON migration behavior still need parity.
7. Schedules and heartbeat: schedule CRUD, strict local cron-range validation, unique labels, id-or-label delete/fire, validation preview, next-fire preview, due execution that is not blocked by older queue backlog, manual fire control, heartbeat loop with per-agent overrides/due status, heartbeat manual tick/state clear/status counters, and default heartbeat interval assignment for newly created agents now exist; full Croner parity, timezone/named cron fields, and settings-file parity are still incomplete.
8. Pairing and channels: pairing persistence/API, sender enforcement, sticky default target routing, Telegram text inbound/outbound bridge, Telegram channel service controls, and pending-response acking now exist; Discord/WhatsApp/SSE client bridges plus Telegram media parity are still missing.
9. Plugin runtime: JSON-config hooks plus executable Python `plugin.py` modules now support incoming/outgoing transforms, activation, logging, and event handlers; JS/TS module loading and full hook/context API parity are still missing.
10. CLI: basic `pocketstudio` command exists for daemon/status/version/send/logs/settings/queue/worker/process/channel/agent/team/provider/schedule/pairing/task/project, including local daemon start/stop/status/restart/open, filtered logs, settings validation/import/export/backup/restore, process list/kill, worker pause/resume/tick/maintenance, Telegram channel status/start/stop/restart/tick, task update/comment/reorder, and project update flows; richer channel setup and interactive flows remain incomplete.
